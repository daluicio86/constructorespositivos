mutation(
  $refId: ID!,
  $ref: String!,
  $field: String!,
  $source: String!,
  $info: FileInfoInput!,
  $file: Upload!
)
{
  upload (
    refId: $refId,
    ref: $ref,
    field: $field,
    source: $source,
    info: $info,
    file: $file
  )
  {
    id
  }
}

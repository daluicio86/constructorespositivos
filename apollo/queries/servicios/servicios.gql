query servicios($id: ID!) {
  categoryServicios(where: { id: $id }) {
    categoria
    servicios(sort: "nombre:asc") {
      id
      nombre
      web
      telefono
      ciudad
      imagen_servicio {
        url
      }
    }
  }
}
